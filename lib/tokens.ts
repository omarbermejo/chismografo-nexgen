// Punto único de verdad para colores que necesita el JS (confeti).
// El resaltador verde es idéntico en ambos temas, así que vive aquí como constante.
export const HIGHLIGHT = '#39e079'
export const MARKER = '#e8407a'
export const PEN = '#2b3a8c'

// Paleta de confeti: tinta + resaltador + marcador (mezcla de cuaderno).
export const CONFETTI_COLORS = [HIGHLIGHT, '#2bc463', '#b6ffd3', MARKER, PEN]
